# Deploy ONLY fund-transfer-service
global:
  namespace: internet-banking

serviceRegistry:
  enabled: false

configServer:
  enabled: false

apiGateway:
  enabled: false

userService:
  enabled: false

coreBankingService:
  enabled: false

fundTransferService:
  enabled: true
  name: internet-banking-fund-transfer-service
  replicaCount: 1
  image:
    repository: saroja21/internet-banking-fund-transfer-service
    tag: "main"
    pullPolicy: Always
  service:
    type: ClusterIP
    port: 8083
  resources:
    requests:
      memory: "512Mi"
      cpu: "100m"
    limits:
      memory: "1Gi"
      cpu: "250m"
  healthCheck:
    enabled: true
    initialDelaySeconds: 240
    periodSeconds: 20
    failureThreshold: 20

utilityPaymentService:
  enabled: false

strategy:
  type: RollingUpdate
  rollingUpdate:
    maxSurge: 1
    maxUnavailable: 0
