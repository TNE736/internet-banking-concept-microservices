# Deploy ONLY service-registry (Eureka)
global:
  namespace: internet-banking

serviceRegistry:
  enabled: true
  name: internet-banking-service-registry
  replicaCount: 1
  image:
    repository: saroja21/internet-banking-service-registry
    tag: "main"
    pullPolicy: Always
  service:
    type: ClusterIP
    port: 8761
  resources:
    requests:
      memory: "512Mi"
      cpu: "100m"
    limits:
      memory: "1Gi"
      cpu: "250m"
  healthCheck:
    enabled: true
    initialDelaySeconds: 240
    periodSeconds: 20
    failureThreshold: 20

configServer:
  enabled: false

apiGateway:
  enabled: false

userService:
  enabled: false

coreBankingService:
  enabled: false

fundTransferService:
  enabled: false

utilityPaymentService:
  enabled: false

strategy:
  type: RollingUpdate
  rollingUpdate:
    maxSurge: 1
    maxUnavailable: 0
